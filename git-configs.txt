Using Github:
codecrafters submit pushes my code
Before submitting do git commit -am "commit-log" to choose the message that will occur

Do:
git push origin master
git push github master (push changes to my github repository)
(Combined is for synchronization)